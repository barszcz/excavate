var mta = require('./lib/mta')();
var parseString = require('xml2js').parseString;
var xml = require('fs').readFileSync(__dirname + '/test/mock.xml', 'utf8');
var util = require('util');




// // Get status for all services
mta.status('BT', function(err, status){
  if (err) throw err;

  console.log(status);
});


// // Get status for the subway system
// mta.status({service: 'subway'}, function(err, status){
//   if (err) throw err;

//   console.log(status);
// });

// // Get status for the subway system
// mta.status('subway', function(err, status){
//   if (err) throw err;

//   console.log(status);
// });
